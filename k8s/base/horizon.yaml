apiVersion: apps/v1
kind: Deployment
metadata:
  name: social-network-horizon
  namespace: social-network-laravel
  labels:
    app: social-network-laravel
    component: horizon
spec:
  replicas: 1
  selector:
    matchLabels:
      app: social-network-laravel
      component: horizon
  template:
    metadata:
      labels:
        app: social-network-laravel
        component: horizon
    spec:
      containers:
      - name: horizon
        image: ghcr.io/liberusoftware/social-network-laravel:latest
        command: ["php", "artisan", "horizon"]
        envFrom:
        - configMapRef:
            name: social-network-config
        - secretRef:
            name: social-network-secrets
        volumeMounts:
        - name: storage
          mountPath: /var/www/html/storage
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          exec:
            command: ["php", "artisan", "horizon:status"]
          initialDelaySeconds: 30
          periodSeconds: 30
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          capabilities:
            drop: [ALL]
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: social-network-storage
      securityContext:
        fsGroup: 1000
