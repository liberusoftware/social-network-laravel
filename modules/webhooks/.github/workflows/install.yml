name: Install

# A release question, not a per-commit one: this resolves the package from
# nothing, the way a consumer first installs it.
on:
  push:
    tags: ['[0-9]+.[0-9]+.[0-9]+']
  workflow_dispatch:

jobs:
  install:
    uses: liberusoftware/.github/.github/workflows/package-install.yml@75d1786646b095ba49360f735f27e0b2fa932b8d
