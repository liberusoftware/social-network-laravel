name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# A publish sweep pushes every package within seconds of the others. Superseding
# this repository's previous run keeps a re-publish from stacking on a sweep that
# has not finished.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  tests:
    uses: liberusoftware/.github/.github/workflows/package-tests.yml@75d1786646b095ba49360f735f27e0b2fa932b8d
    with:
      phpstan-level: 10
      coverage-threshold: 16
