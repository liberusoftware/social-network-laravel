# Liberu Social Network Profiles

Provider-neutral profiles for handles, bios, attributes, avatars, verification,
visibility, blocking, and lifecycle state. The module owns its persistence and
events and exposes authorization through host-defined Laravel Gate abilities.
HTTP, Filament, and Livewire adapters are separate one-to-one packages.
